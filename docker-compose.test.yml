version: '2'
services:
  db_test:
    image: postgres
    environment:
      POSTGRES_DB: ziltag_test
  app:
    build: .
    image: ziltag
    volumes:
     - .:/ziltag
    depends_on:
     - db_test
