<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Ziltag - Join for free.</title>
  <%= render 'shared/meta' %>

  <%= favicon_link_tag 'favicon.png' %>
  <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
  <%= stylesheet_link_tag 'app', media: 'all' if Rails.env.production? %>
  <%= csrf_meta_tags %>
  <%= content_for :head if content_for? :head %>
</head>
<body class="<%= "#{controller_path} #{action_name}" %>">
  <div class="mdl-layout mdl-js-layout mdl-layout--fixed-header mdl-color--white">
    <%= render 'shared/header' %>
    <%= yield :before_main if content_for?(:before_main) %>
    <main class="mdl-layout__content">
      <%= content_for?(:main) ? yield(:main) : yield %>
    </main>
  </div>
  <%= render 'shared/uservoice' %>
  <%= javascript_include_tag 'app' %>
</body>
</html>