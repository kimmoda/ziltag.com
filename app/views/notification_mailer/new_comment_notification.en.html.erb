<p>Dear <b><%= @user.username %></b>,</p>
<p>There is a new comment on your tag!</p>
<%= image_tag @comment.ziltag.share_image, class: 'mail__share-image' %>
<div class="mail__comment">
  <%= image_tag @comment.user.avatar.thumb.url, class: 'mail__comment-avatar' %>
  <div class="mail__comment-container">
    <div class="mail__comment-username"><%= @comment.user.username %></div>
    <div class="mail__comment-time"><%= time_ago_in_words @comment.created_at %></div>
    <div class="mail__comment-content"><%= @comment.content %></div>
  </div>
  <%= link_to url_for("/ziltags/#{@comment.ziltag.slug}"), class: 'mail__reply' do %>
    <%= image_tag 'reply.png' %>Reply Comment
  <% end %>
  <div class="mail__unfollow">Don't want to receive notification from this thread? <%= link_to 'Unfollow', unsubscribe_url(token: @unsubscribe_token) %></div>
</div>