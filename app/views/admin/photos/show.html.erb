<p>
  <strong><%= Admin::Photo.human_attribute_name(:user) %>:</strong>
  <%= link_to @admin_photo.user, [:admin, @admin_photo.user] %>
</p>

<p>
  <strong><%= Admin::Photo.human_attribute_name(:title) %>:</strong>
  <%= @admin_photo.title %>
</p>

<p>
  <strong><%= Admin::Photo.human_attribute_name(:image) %>:</strong>
  <%= link_to image_tag(@admin_photo.image.thumb), @admin_photo.image_url %>
</p>

<div class="page-header">
  <h2><%= Post.model_name.human %></h2>
</div>
<%= image_tag(@admin_photo.image_url, data: {sticker: '', new_post: new_admin_post_path}) %>
<p>點擊圖片任一處新增樂貼</p>

<div class="page-header">
  <h2><%= Comment.model_name.human %></h2>
</div>
<div style="position:relative">
  <%= image_tag(@admin_photo.image_url, data: {comment_sticker: '', new_comment: new_admin_comment_path}) %>
  <div style="position:absolute;left:10px;top:10px">
    <div class="comment_sticker"><%= image_tag User.first.avatar.thumb, width: 20 %> hello</div>
  </div>
</div>
<p>點擊圖片任一處新增留言</p>

<hr>

<%= link_to t('scaffold.edit'), edit_admin_photo_path(@admin_photo), class: 'btn btn-default' %>
<%= link_to t('scaffold.back'), admin_photos_path, class: 'btn btn-default' %>
