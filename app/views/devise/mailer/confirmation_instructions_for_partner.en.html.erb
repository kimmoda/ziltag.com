<p>Dear <b><%= @resource.username %></b>,</p>
<p>Thank you for joining Ziltag Partner. Here is your registration details.</p>
<table class="mail__table">
  <tbody align="left">
    <tr>
      <th>Username</th>
      <td><%= @resource.username %></td>
    </tr>
    <tr>
      <th>Email</th>
      <td><%= @email %></td>
    </tr>
    <tr>
      <th>Domain</th>
      <td><%= @box.url %></td>
    </tr>
  </tbody>
</table>
<textarea wrap="off" rows="1" readonly="readonly" class="mail__script"><script src="<%= javascript_path '/plugin.js' %>" data-ziltag="<%= @box.token %>"></script></textarea>

<div class="mail__confirm">
  <p>In order to get started, please confirm your email address.</p>
  <%= link_to 'Confirm Email', confirmation_url(@resource, confirmation_token: @token), class: 'mail__button' %>
</div>